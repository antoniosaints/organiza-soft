services:
  app:
    build: .
    ports:
      - "3340:5000"
    env_file:
      - .env_prod
    volumes:
      - .:/app
    command: sh -c "npm install && npm run migrate:dev && npm run seed && npm run dev"
